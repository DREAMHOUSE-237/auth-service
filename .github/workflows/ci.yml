name: CI and Build Docker Image

on:
  push:
    branches:
      - dev   # déclenchement uniquement sur la branche dev

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      # 1. Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Lancer les tests (à adapter selon ton stack)
      - name: Run tests
        run: |
          echo "Exécute tes tests ici (npm test, mvn test, pytest, etc.)"

      # 3. Se connecter à Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 4. Construire et tagger l’image
      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/auth-service:${{ github.sha }} .
          docker tag ${{ secrets.DOCKERHUB_USERNAME }}/auth-service:${{ github.sha }} ${{ secrets.DOCKERHUB_USERNAME }}/auth-service:latest

      # 5. Pousser l’image vers Docker Hub
      - name: Push Docker image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/auth-service:${{ github.sha }}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/auth-service:latest

  auto-merge:
    needs: test-and-build
    runs-on: ubuntu-latest
    steps:
      - name: Merge dev into main
        uses: devops-infra/action-pull-request@v0.5.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          source_branch: dev
          target_branch: main
          title: "Auto merge from dev to main"
          body: "CI passed, merging dev into main for deployment"

  notify-infrastructure:
    needs: auto-merge
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy workflow in infrastructure repo
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: DREAMHOUSE-237/infrastructure   # ⚠️ remplace par le vrai nom de ton organisation et repo
          event-type: deploy-auth-service

